Create a local gitconfig to overwrite global general configurations like

```bash
[user]
	name = <your name> 
	email = <your-email> 
	signingkey = **** 
```

and save this file as .gitconfig.local in root
